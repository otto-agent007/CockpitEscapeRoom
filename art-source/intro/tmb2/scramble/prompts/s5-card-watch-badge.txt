Generate exactly ONE image using your BUILT-IN `image_gen` tool.

Constraints on HOW you generate:
- Built-in `image_gen` only. Do NOT use any MCP server and do NOT use `scripts/image_gen.py` or
  anything reading OPENAI_API_KEY. This must run on the ChatGPT plan. If the built-in tool is
  unavailable, stop and say so.
- Size 1536x1024 landscape, highest quality. Generate once, no variants.
- Report the NATIVE size the tool returned and do NOT resize or upscale the result.
- TWO reference images are attached: first the PREVIOUS VERSION of this same shot (the authority
  for everything except the one change), second another card showing the SAME captain's hat
  whose round badge design must be copied exactly.

Then copy the result to exactly:
  /mnt/2TBHDD/CockpitEscapeRoom/art-source/intro/tmb2/scramble/generated/s5-card-watch.png
Print the absolute path and pixel dimensions. Change no other file, do not run git.

=== THE IMAGE PROMPT (use verbatim) ===

Reproduce the FIRST attached reference image EXACTLY — the same pilot fastening the same steel
chronograph watch, the same captain's hat with its charcoal-navy crown, braided gold chin cord
and black visor, the same face, lamp, framing, light and palette — with ONE change only: the
badge on the crown front is replaced by the round cap badge shown on the hat in the SECOND
reference image: a red outer ring, a cream inner disc, a deep blue centre, and small gold wings
across it, drawn as clean flat shapes. Nothing else changes.

Colour and shading rules, followed strictly:
- Use at most three flat shades per material, every region one flat colour with a hard edge.
- No gradients, no glow, no texture, no noise, no dithering.

Absolutely NO text anywhere: no letters, no numbers, no monograms, no watermark, no border,
no caption. The badge is pure shapes.
