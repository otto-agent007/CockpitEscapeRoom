Generate exactly ONE image using your BUILT-IN `image_gen` tool.

Constraints on HOW you generate:
- Built-in `image_gen` only. Do NOT use any MCP server and do NOT use `scripts/image_gen.py` or
  anything reading OPENAI_API_KEY. This must run on the ChatGPT plan. If the built-in tool is
  unavailable, stop and say so.
- Size 1536x1024 landscape, highest quality. Generate once, no variants.
- Report the NATIVE size the tool returned and do NOT resize or upscale the result.
- One reference image is attached: the PREVIOUS FRAME of this same shot. Reproduce its scene,
  framing, hat design, style and colours EXACTLY, changing only what the prompt states. It is
  the authority for everything else.

Then copy the result to exactly:
  /mnt/2TBHDD/CockpitEscapeRoom/art-source/intro/tmb2/scramble/generated/s4-card-cap-mid2.png
Print the absolute path and pixel dimensions. Change no other file, do not run git.

=== THE IMAGE PROMPT (use verbatim) ===

Reproduce the attached reference
image EXACTLY — the same captain's hat with its silver chin cord, black embroidered visor and
round badge, the same waiting upturned hand, the same framing, light and palette — with ONE
change only: the hat has fallen to the MIDDLE of the frame, halfway between its high position
and the waiting hand, rotated nearly level as it drops. Nothing else changes.

Colour and shading rules, followed strictly:
- Use at most three flat shades per material: a base, one darker shade, one lighter shade.
- Every colour region is a solid area of ONE flat colour with a hard edge. No gradients, no
  blending, no soft edges, no glow, no bloom. Any light pool is one broad flat lighter shape.
- NO specular highlights beyond ONE flat pale wedge allowed on the glossy visor, NO texture,
  NO noise, NO dithering, NO grain.

Palette to draw from: near-black #0A0F22, charcoal navy #23262E, black #101014, panel navy
#181E36, silver #C9CDD6, off-white #FFFDF0, signal red #C8102E, deep blue #1A3C8C, gold
#F5C424, skin #F8AC75.

Absolutely NO text anywhere: no letters, no numbers, no monograms, no watermark, no border,
no caption. The cap badge is drawn as pure shapes.
