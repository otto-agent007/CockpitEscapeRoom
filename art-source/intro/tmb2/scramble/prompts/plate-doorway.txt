Generate exactly ONE image using your BUILT-IN `image_gen` tool.

Constraints on HOW you generate:
- Built-in `image_gen` only. Do NOT use any MCP server and do NOT use `scripts/image_gen.py` or
  anything reading OPENAI_API_KEY. This must run on the ChatGPT plan. If the built-in tool is
  unavailable, stop and say so.
- Size 1536x1024 landscape, highest quality. Generate once, no variants.
- Report the NATIVE size the tool returned and do NOT resize or upscale the result.
- One reference image is attached: a rough placeholder layout mock. Follow it for COMPOSITION
  ONLY (a tall bright doorway centred in a dark frame); its blocky style, colours and any text
  overlays must be ignored, and no text from it may appear in the output.

Then copy the result to exactly:
  /mnt/2TBHDD/CockpitEscapeRoom/art-source/intro/tmb2/scramble/generated/plate-doorway.png
Print the absolute path and pixel dimensions. Change no other file, do not run git.

=== THE IMAGE PROMPT (use verbatim) ===

A flat cel-shaded 2D background illustration in clean 16-bit video-game cutscene style: the tall
doorway of an aircraft hangar at night, seen straight on from the dark apron outside.

Scene: the frame is mostly near-black darkness. Centred is the hangar's tall rectangular doorway
opening, glowing with warm-white interior floodlight that spills a long bright pool onto the
ground toward the viewer. Inside the lit opening, partially visible, the rounded nose and forward
fuselage of a McDonnell Douglas DC-9 jetliner: light warm-grey upper fuselage over white lower
fuselage split at the window line, a simple cockpit-window band, standing on a twin-wheel nose
gear strut. The doorway is FULLY OPEN — no doors visible in the opening (sliding door leaves are
a separate element). Around the doorway, the hangar's dark outer wall with simple structural
ribs, and above it one small amber beacon lamp.

Colour and shading rules, followed strictly:
- Use at most three flat shades per material: a base, one darker shade, one lighter shade.
- Every colour region is a solid area of ONE flat colour with a hard edge. No gradients, no
  blending, no soft edges, no glow, no bloom. The light spill is a broad flat lighter shape.
- NO specular highlights, NO shine streaks, NO texture, NO noise, NO dithering, NO grain.

Palette to draw from: near-black #0A0F22, hangar blue #141A2E, panel navy #181E36, steel blue
#323E60, off-white #FFFDF0, pale light #C8D2E8, warm grey #B9BCC0, amber #FFB020.

Mood: the threshold moment — the lit doorway waiting in the dark.

Absolutely NO text anywhere: no letters, no numbers, no logos, no placards, no watermark, no
border, no caption.
