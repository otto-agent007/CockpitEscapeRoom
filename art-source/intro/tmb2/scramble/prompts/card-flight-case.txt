Generate exactly ONE image using your BUILT-IN `image_gen` tool.

Constraints on HOW you generate:
- Built-in `image_gen` only. Do NOT use any MCP server and do NOT use `scripts/image_gen.py` or
  anything reading OPENAI_API_KEY. This must run on the ChatGPT plan. If the built-in tool is
  unavailable, stop and say so.
- Size 1536x1024 landscape, highest quality. Generate once, no variants.
- Report the NATIVE size the tool returned and do NOT resize or upscale the result.
- One reference image is attached: a rough placeholder layout mock. Follow it for COMPOSITION
  ONLY (a centred subject under a light pool with dark surroundings); its blocky style, colours
  and any text overlays in it must be ignored, and no text from it may appear in the output.

Then copy the result to exactly:
  /mnt/2TBHDD/CockpitEscapeRoom/art-source/intro/tmb2/scramble/generated/card-flight-case.png
Print the absolute path and pixel dimensions. Change no other file, do not run git.

=== THE IMAGE PROMPT (use verbatim) ===

A flat cel-shaded 2D close-up illustration in clean 16-bit video-game cutscene style: a single
dramatic still of an airline captain's flight case, centred, at night.

Scene: a modern hard-sided pilot flight case sitting on a steel crew-room bench, seen slightly
from above and to the front. Slim rectangular briefcase profile with softly rounded corners,
matte black composite shell with clean panel lines, a low-profile brushed-steel handle folded
flat on top, and TWO brushed-steel latches on the front face, both OPEN with their clasps flipped
up. A single thin signal-red accent line runs horizontally across the front face. Between the
latches sits a small blank rectangular brushed-steel nameplate — completely BLANK metal, no
engraving, no letters. A single overhead light pool illuminates the case; the surroundings fall
off into near-black darkness. The bench edge reads as one simple steel line.

Colour and shading rules, followed strictly:
- Use at most three flat shades per material: a base, one darker shade, one lighter shade.
- Every colour region is a solid area of ONE flat colour with a hard edge. No gradients, no
  blending, no soft edges, no glow, no bloom.
- NO specular highlights, NO shine streaks, NO gloss on the leather. The light pool is a broad
  flat lighter region with a hard edge, in the style of cel animation.
- NO leather grain, NO texture, NO noise, NO speckling, NO dithering, NO stippling.

Palette to draw from: near-black #0A0F22, matte black #16161C, hangar blue #141A2E, panel navy
#181E36, steel blue #323E60, brushed steel #9AA2B4, off-white #FFFDF0, signal red #C8102E,
amber #FFB020.

Mood: quiet ritual before the flight — an heirloom about to go to work.

Absolutely NO text anywhere: no letters, no numbers, no logos, no monograms, no placards, no
watermark, no border, no caption. The nameplate is blank.
