Generate exactly ONE image using your BUILT-IN `image_gen` tool.

Constraints on HOW you generate:
- Built-in `image_gen` only. Do NOT use any MCP server and do NOT use `scripts/image_gen.py` or
  anything reading OPENAI_API_KEY. This must run on the ChatGPT plan. If the built-in tool is
  unavailable, stop and say so.
- Size 1536x1024 landscape, highest quality. Generate once, no variants.
- Report the NATIVE size the tool returned and do NOT resize or upscale the result.
- One reference image is attached: a rough placeholder layout mock. Follow it for COMPOSITION
  ONLY (a single centred subject under a light pool against darkness); its blocky style, colours
  and any text overlays must be ignored, and no text from it may appear in the output.

Then copy the result to exactly:
  /mnt/2TBHDD/CockpitEscapeRoom/art-source/intro/tmb2/scramble/generated/s4-card-logbook.png
Print the absolute path and pixel dimensions. Change no other file, do not run git.

=== THE IMAGE PROMPT (use verbatim) ===

A flat cel-shaded 2D close-up illustration in clean 16-bit video-game cutscene style, a single
dramatic still at night: a pilot's logbook in the instant of being snapped shut — a thick,
well-worn dark navy leather logbook seen from a low three-quarter angle on a steel crew-room
table, its cover falling closed at a slight angle with a few blank cream page edges still
fanning out of the closing gap, one strong hand pressing the cover down flat. The leather is
aged at the corners with one darker worn shade; a plain gold band runs across the lower cover
with NO lettering on it. One warm overhead light pool; the surroundings fall to near-black.

Colour and shading rules, followed strictly:
- Use at most three flat shades per material: a base, one darker shade, one lighter shade.
- Every colour region is a solid area of ONE flat colour with a hard edge. No gradients, no
  blending, no soft edges, no glow, no bloom. Any light pool is one broad flat lighter shape.
- NO specular highlights, NO shine streaks, NO texture, NO noise, NO dithering, NO grain.

Palette to draw from: near-black #0A0F22, hangar blue #141A2E, panel navy #181E36, steel blue
#323E60, off-white #FFFDF0, cream pages #EFE8D4, uniform navy #1A203F, worn leather #232B4A,
gold #F5C424, dark gold #CB8A06, skin #F8AC75.

Absolutely NO text anywhere: no letters, no numbers, no logos, no placards, no monograms, no
watermark, no border, no caption. The logbook cover and its gold band are completely blank.
