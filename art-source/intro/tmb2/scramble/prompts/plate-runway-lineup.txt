Generate exactly ONE image using your BUILT-IN `image_gen` tool.

Constraints on HOW you generate:
- Built-in `image_gen` only. Do NOT use any MCP server and do NOT use `scripts/image_gen.py` or
  anything reading OPENAI_API_KEY. This must run on the ChatGPT plan. If the built-in tool is
  unavailable, stop and say so.
- Size 1536x1024 landscape, highest quality. Generate once, no variants.
- Report the NATIVE size the tool returned and do NOT resize or upscale the result.
- One reference image is attached: a rough placeholder layout mock. Follow it for COMPOSITION
  ONLY (a runway converging to a centred vanishing point under a night sky); its blocky style,
  colours and any text overlays must be ignored, and no text from it may appear in the output.

Then copy the result to exactly:
  /mnt/2TBHDD/CockpitEscapeRoom/art-source/intro/tmb2/scramble/generated/plate-runway-lineup.png
Print the absolute path and pixel dimensions. Change no other file, do not run git.

=== THE IMAGE PROMPT (use verbatim) ===

A flat cel-shaded 2D background illustration in clean 16-bit video-game cutscene style: an empty
airport runway at night, viewed from the centreline at threshold height, converging to a
vanishing point at the centre of the horizon.

Scene, matching the attached layout reference: the dark asphalt runway fills the lower half of
the frame as one broad trapezoid converging to the horizon. IMPORTANT: the runway surface is
completely BARE — NO centreline markings, NO painted stripes, NO numbers, NO edge lights, NO
lamps of any kind on or beside the runway. All runway lighting is added separately at runtime.
At the horizon, a thin band of distant airport glow: a few tiny flat silhouettes of buildings and
a control tower against a faint warm haze line. Above, a clear night sky filling the upper half:
near-black blue with small crisp single-pixel-style stars, and one or two very thin flat cloud
streaks.

Colour and shading rules, followed strictly:
- Use at most three flat shades per material: a base, one darker shade, one lighter shade.
- Every colour region is a solid area of ONE flat colour with a hard edge. No gradients, no
  blending, no soft edges, no glow, no bloom. The horizon haze is one flat pale band.
- NO texture, NO noise, NO speckling, NO dithering, NO grain.

Palette to draw from: near-black #0A0F22, asphalt #1E2233, hangar blue #141A2E, steel blue
#323E60, off-white #FFFDF0, pale haze #6A7490, amber #FFB020.

Mood: the held breath before the roll — an empty runway waiting in the dark.

Absolutely NO text anywhere: no letters, no numbers, no runway markings, no logos, no watermark,
no border, no caption.
