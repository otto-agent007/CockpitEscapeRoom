Generate exactly ONE image using your BUILT-IN `image_gen` tool.

Constraints on HOW you generate:
- Built-in `image_gen` only. Do NOT use any MCP server and do NOT use `scripts/image_gen.py` or
  anything reading OPENAI_API_KEY. This must run on the ChatGPT plan. If the built-in tool is
  unavailable, stop and say so.
- Size 1536x1024 landscape, highest quality. Generate once, no variants.
- Report the NATIVE size the tool returned and do NOT resize or upscale the result.
- One reference image is attached: a rough placeholder layout mock. Follow it for COMPOSITION
  ONLY (open starfield, faint glow low in the frame); its blocky style, colours and any text
  overlays must be ignored, and no text from it may appear in the output.

Then copy the result to exactly:
  /mnt/2TBHDD/CockpitEscapeRoom/art-source/intro/tmb2/scramble/generated/plate-night-sky.png
Print the absolute path and pixel dimensions. Change no other file, do not run git.

=== THE IMAGE PROMPT (use verbatim) ===

A flat cel-shaded 2D background illustration in clean 16-bit video-game cutscene style: a wide
open night sky, nearly full frame, seen from altitude.

Scene: deep near-black blue sky covering the whole frame, scattered with small crisp stars of
one or two pixels' weight at this scale — some slightly brighter, none glowing. Across the lower
fifth of the frame, a very faint flat band of warm horizon glow with two or three long, thin,
flat cloud streaks in dark blue-grey crossing it. The upper three quarters of the frame stay
open and uncluttered — a title emblem and a climbing aircraft will be composited there at
runtime. No moon, no ground, no buildings.

Colour and shading rules, followed strictly:
- Use at most three flat shades per material: a base, one darker shade, one lighter shade.
- Every colour region is a solid area of ONE flat colour with a hard edge. No gradients, no
  blending, no soft edges, no glow, no bloom. The horizon glow is one flat band.
- NO texture, NO noise, NO speckling, NO dithering, NO grain, NO lens effects.

Palette to draw from: near-black #0A0F22, night blue #10162B, steel blue #323E60, off-white
#FFFDF0, pale haze #6A7490, warm glow #6A5030.

Mood: the quiet top of the climb — stars, thin cloud, nothing else.

Absolutely NO text anywhere: no letters, no numbers, no logos, no watermark, no border, no
caption.
