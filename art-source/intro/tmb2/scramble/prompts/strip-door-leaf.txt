Generate exactly ONE image using your BUILT-IN `image_gen` tool.

Constraints on HOW you generate:
- Built-in `image_gen` only. Do NOT use any MCP server and do NOT use `scripts/image_gen.py` or
  anything reading OPENAI_API_KEY. This must run on the ChatGPT plan. If the built-in tool is
  unavailable, stop and say so.
- Size 1024x1536 PORTRAIT, highest quality. Generate once, no variants.
- Report the NATIVE size the tool returned and do NOT resize or upscale the result.
- No reference image for this one; it is a flat element, not a scene.

Then copy the result to exactly:
  /mnt/2TBHDD/CockpitEscapeRoom/art-source/intro/tmb2/scramble/generated/strip-door-leaf.png
Print the absolute path and pixel dimensions. Change no other file, do not run git.

=== THE IMAGE PROMPT (use verbatim) ===

A flat cel-shaded 2D game element in clean 16-bit video-game style: one sliding hangar-door leaf,
face on, filling the ENTIRE canvas edge to edge as a flat surface. This is a texture element, not
a scene — no floor, no sky, no perspective, no vanishing lines, no shadow cast on anything.

The door leaf: dark blue-grey industrial steel, divided into large flat rectangular panels by
slightly darker recessed seams, with three evenly spaced horizontal reinforcement ribs running
the full width. Along the RIGHT edge of the canvas, a single vertical warning stripe of diagonal
yellow-and-black hazard chevrons, about one tenth of the canvas width — this is the door's
leading edge. A few simple flat bolt heads at the rib intersections, drawn as small darker
circles.

Colour and shading rules, followed strictly:
- Use at most three flat shades per material: a base, one darker shade, one lighter shade.
- Every colour region is a solid area of ONE flat colour with a hard edge. No gradients, no
  blending, no soft edges, no glow.
- NO specular highlights, NO rust, NO wear stains, NO scratches, NO texture, NO noise, NO
  dithering, NO grain.

Palette to draw from: near-black #0A0F22, hangar blue #141A2E, panel navy #181E36, steel blue
#323E60, gold-yellow #F5C424, near-black stripe #16161C.

Absolutely NO text anywhere: no letters, no numbers, no logos, no placards, no watermark, no
border, no caption.
